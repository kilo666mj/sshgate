sshgate
sshgate-linux-*
db.json
sshgate.db
sshgate.db-*
*.tmp

# Real ansible inventory holds host names; commit inventory.example instead.
ansible/inventory
ansible/host_vars/
ansible/group_vars/sshgate.yml

# Stray npm output. Nothing here is a Node project; an npm install run in the
# wrong directory once left 5 MB of node_modules next to the Jinja templates.
node_modules/
package.json
package-lock.json
